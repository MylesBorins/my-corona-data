{
  "name": "my-corona-data",
  "version": "1.0.0",
  "description": "Just looking into data",
  "main": "index.js",
  "scripts": {
    "start": "node --experimental-modules index.mjs",
    "update-data": "echo update-subtree fetch-toronto-data fetch-state-data | xargs -n1 npm run",
    "fetch-toronto-data": "curl -L https://ckan0.cf.opendata.inter.prod-toronto.ca/download_resource/e5bf35bc-e681-43da-b2ce-0242d00922ad?format=json > third_party/toronto-data/toronto-covid-cases.json && git diff-index --quiet HEAD || git commit -m 'update toronto data'",
    "fetch-state-data": "node ./scripts/update-state-data.mjs && git add -A && git diff-index --quiet HEAD || git commit -m 'update state data'",
    "update-subtree": "git subtree pull --prefix third_party/coronavirus-data https://github.com/nychealth/coronavirus-data master --squash -m \"update city data\"",
    "test": "npm run lint",
    "lint": "eslint *.mjs lib/*.mjs",
    "fix-lint": "npm run lint -- --fix"
  },
  "author": "Myles Borins <mylesborins@google.com>",
  "license": "Apache-2.0",
  "dependencies": {
    "csv-parse": "^4.8.8",
    "node-fetch": "^2.6.0"
  },
  "devDependencies": {
    "babel-eslint": "^10.1.0",
    "eslint": "^7.5.0"
  }
}
